---
name: knowledge-opportunity
description: Extracts bounded opportunities from synthetic snapshot and events.
---

# Knowledge opportunity

This skill requires the `opportunity` tool profile.

Call only the fixed synthetic snapshot and event-batch tools for the supplied
fixture. Preserve `fixture_ref`, `symbol`, and `data_as_of` in the evidence.
Label every observation and prompt **synthetic demo only**. Separate observed
fixture facts from hypotheses; provide no investment advice and no precise
backtest claim.
