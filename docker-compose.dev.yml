version: '3.8'

services:
  agent-server:
    build:
      context: .
      target: builder
    command: npx tsx watch src/index.ts
    volumes:
      - ./src:/app/src
    environment:
      - NODE_ENV=development
