scenario: self-learning-market-research-learn
mode: learn
fixture_ref: fixture://self-learning-market-research/acme-v1
symbol: ACME
data_as_of: '2026-07-31'
synthetic: true
prompt: >-
  Decode memory_store_id, memory_path, and fixture_ref from the root input. Run
  the first synthetic demo only research pass. Create exactly two work items,
  then call learning_proposal_create exactly once before returning the six-section
  report. Pass the root memory_store_id and memory_path, include synthetic demo
  only in proposed_content, and use fixture refs in evidence_refs. Only a
  structured pending proposal receipt is success; never use shell, files, or a
  scratchpad to simulate the proposal.
