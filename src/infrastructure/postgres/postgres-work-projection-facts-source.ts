import { PostgresWorkProjectionFactsQuery } from './postgres-work-projection-facts-query.js';

/** Compatibility name for the S2 read source; the typed C query is authoritative. */
export class PostgresWorkProjectionFactsSource extends PostgresWorkProjectionFactsQuery {}
